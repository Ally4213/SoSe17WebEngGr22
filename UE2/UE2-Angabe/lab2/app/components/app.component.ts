import { Component } from '@angular/core';
import { FooterComponent } from './footer.component';

@Component({
  moduleId: module.id,
  selector: 'my-app',
  templateUrl: '../views/app.component.html',
})
export class AppComponent {
  title: string = "BIG Smart Home";
}
